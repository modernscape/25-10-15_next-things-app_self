import { create } from "zustand";
import { persist } from "zustand/middleware";

// export default function useThingsStore() {
//   const [trashed, setTrashed] = useState(false);

//   return (
//       setTrashed: (state) => {trashed = !state};

//   )

// }
